import { expect } from 'chai';

describe('test', () => {
  it('should describe', () => {
    browser.url('www.google.com');
  });

  it('should true be truthy', () => {
    expect(true).true;
  });
});
